package model;

import model.Move;
import model.Player;

public class Action {
    public Move move;
    public Player player;

    public Action(Player player, Move m) {
        this.move = move;
        this.player = player;
    }
}
